# Example Secret — create out-of-band (ESO, SealedSecrets, or kubectl).
# The base chart never renders this file.
apiVersion: v1
kind: Secret
metadata:
  name: prism-store-credentials
  namespace: prism-store
type: Opaque
stringData:
  ingest-token: replace-me-ingest
  admin-token: replace-me-admin
